## 1. CLI Registration

- [x] 1.1 Add `.alias("checkout")` to the `add` command in `src/cli.ts`

## 2. Tests

- [x] 2.1 Existing `checkout.test.ts` covers `checkoutCommand` — alias is a Commander.js built-in, verified via `--help` output
