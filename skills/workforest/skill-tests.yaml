skill: git-workforest:workforest
model: haiku

tests:
  - id: workforest-mention
    prompt: "how does workforest work?"
    should_trigger: true

  - id: git-forest-mention
    prompt: "run git forest status"
    should_trigger: true

  - id: unrelated
    prompt: "how do I rebase my branch?"
    should_trigger: false
