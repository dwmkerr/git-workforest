skill: git-workforest:workforest
model: haiku

tests:
  - id: git-forest-list
    prompt: "git forest list"
    should_trigger: true

  - id: git-forest-add
    prompt: "git forest add feat/new-feature"
    should_trigger: true

  - id: unrelated-git
    prompt: "how do I rebase my branch?"
    should_trigger: false

  - id: unrelated-npm
    prompt: "how do I publish an npm package?"
    should_trigger: false
